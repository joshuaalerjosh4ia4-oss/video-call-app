This project uses React Navigation (see `src/navigation`) rather than
Expo Router, per the required tech stack. This `app/` directory is kept
for structural parity with the requested project layout but intentionally
contains no routes - the app's entry point is `App.tsx` at the project root.
