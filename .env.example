# Used by docker-compose.yml to configure the PostgreSQL container.
# Copy this file to ".env" in the project root (same folder as
# docker-compose.yml) before running `docker compose up -d`.
POSTGRES_USER=videocall
POSTGRES_PASSWORD=videocall_password
POSTGRES_DB=videocall_db
POSTGRES_PORT=5432
